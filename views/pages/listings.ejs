<div class="jumbotron text-center">
  <h1>All Listings</h1>
</div>

<% if (success.length > 0) { %>
    <div class="alert alert-success"><%= success %></div>
<% } %>

<table class="table table-bordered table-hover table-striped">
    <thead>
        <tr>
            <th>Listing Name</th>
            <th>Description</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        
        <% for (var listing of listings) { %>
            <tr>
                <td><%= listing.name %></td>
                <td><%= listing.description %></td>
                <td>
                    <a href="/listings/<%= listing.slug %>" class="btn btn-sm btn-primary">View</a>
                    <a href="/listings/<%= listing.slug %>/edit" class="btn btn-sm btn-primary">Edit</a>
                    <a href="/listings/<%= listing.slug %>/delete" class="btn btn-sm btn-danger">Delete</a>
                </td>
            </tr>
        <% } %>

    </tbody>
</table>